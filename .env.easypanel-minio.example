# ============================================================================
# Voxen — template de env pra deploy Easypanel + MinIO externo
# ============================================================================
# Copie pra Easypanel UI → Environment do App Voxen. Gere os secrets com:
#   openssl rand -base64 32
#
# Produção: prefira Source → Docker image (`ghcr.io/yefclub/voxen:dev` ou
# `ghcr.io/yefclub/voxen:latest`). No Source GitHub/Dockerfile, o Easypanel
# expõe Environment também no build-time e pode mostrar secrets em logs de build.
#
# Guia visual: docs/DEPLOY.md → seção "Easypanel App"
# ============================================================================

# === Infraestrutura básica ===
APP_BASE_URL=https://voxen.seudominio.com
NODE_ENV=production

# Auth (better-auth) — secret de assinatura de sessões
BETTER_AUTH_SECRET=__GERE_COM_openssl_rand_base64_32__

# Master key AES-256-GCM — base64 de 32 bytes. NÃO perca esse valor.
MASTER_KEY=__GERE_COM_openssl_rand_base64_32__

# === Postgres + Redis (serviços Easypanel separados) ===
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB
REDIS_URL=redis://:PASSWORD@HOST:6379/0

# === Storage S3 (MinIO no Easypanel) ===
# URL interna do Easypanel. Troque pelo host interno exibido no painel do MinIO.
# Exemplo no projeto taskivus: http://taskivus-minio:9000
# Se usar domínio/proxy para S3, prefira https://s3.seudominio.com sem :9000.
# O endpoint precisa aceitar API S3 completa, incluindo HEAD Bucket e PUT Object.
S3_ENDPOINT=http://taskivus-minio:9000
S3_ACCESS_KEY=__access_key_do_MinIO__
S3_SECRET_KEY=__secret_key_do_MinIO__
S3_BUCKET=voxen-transcripts
S3_REGION=us-east-1
S3_FORCE_PATH_STYLE=true

# === Build args públicos opcionais ===
# Não coloque secrets em build args. Secrets ficam apenas como env de runtime.
# VOXEN_VERSION=1.0.0
# VOXEN_GIT_SHA=__sha_curto__
# VOXEN_BUILT_AT=__iso_timestamp__
