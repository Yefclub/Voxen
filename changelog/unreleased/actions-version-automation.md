---
tipo: fix
titulo_en: Protected version automation works with read-only defaults
titulo_pt_br: Automação protegida de versão funciona com padrões somente leitura
---

GitHub Actions keeps read-only permissions by default while the repository
policy now permits the version workflow to create its protected pull request.

<!-- pt-BR -->

O GitHub Actions mantém permissões somente leitura por padrão, enquanto a
política do repositório permite que o fluxo de versão crie seu pull request
protegido.
